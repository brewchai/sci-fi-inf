[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimum coverage thresholds (for future)
# addopts = --cov=app --cov-report=term-missing --cov-fail-under=60
