# Database Configuration (Cloud - Supabase/Neon)
# Paste your full connection string here:
SQLALCHEMY_DATABASE_URI="postgresql://user:password@host:port/database"

# Database Configuration (Local - Optional fallback)
# POSTGRES_SERVER=localhost
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_DB=dailydiscovery

# OpenAI (Required for Summarization)
OPENAI_API_KEY=sk-your-key-here

# OpenAlex (Optional but recommended for faster/polite access)
OPENALEX_MAILTO=your-email@example.com

# Redis (Optional for MVP, required for async tasks later)
REDIS_HOST=localhost
REDIS_PORT=6379
